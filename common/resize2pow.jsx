/************************************************************【概要】    PSDファイルを開いた状態で実行すると、カンバスを２のべき乗値にリサイズします。***********************************************************/var doc = activeDocument;if(doc){    var newWidth = calculatePow(doc.width);    var newHeight = calculatePow(doc.height);    if(newWidth && newHeight){        doc.resizeCanvas(newWidth,newHeight,AnchorPosition.BOTTOMCENTER);    }}function calculatePow(size){    for(i = 2; i < 20 ; i++){        if(size == Math.pow(2,i)){            return size;            break;        }else if(size < Math.pow(2,i)){            if(i>=1 && (size - Math.pow(2,i-1)) > ( Math.abs(Math.pow(2,i) - size)) ){                                return Math.pow(2,i);                 }else{                return Math.pow(2,i-1);                                            }                        break;        }    }}